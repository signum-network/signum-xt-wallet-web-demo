// Connect status Label
export const connectionStatusLabel: HTMLElement = document.getElementById(
  "connection-status-label"
) as HTMLParagraphElement;

// Selected Network Label
export const selectedNetworkLabel: HTMLElement = document.getElementById(
  "selected-network-label"
) as HTMLSpanElement;

// Required Network Label
export const requiredNetworkLabel: HTMLElement = document.getElementById(
  "required-network-label"
) as HTMLSpanElement;

// Connect Wallet Button
export const connectWalletButton: HTMLElement = document.getElementById(
  "connect-wallet-button"
) as HTMLButtonElement;

// Disconnect Wallet Button
export const disconnectWalletButton: HTMLElement = document.getElementById(
  "disconnect-wallet-button"
) as HTMLButtonElement;

// Authenticated user Box Section
export const authenticatedUserBox: HTMLElement = document.getElementById(
  "authenticated-user-box"
) as HTMLDivElement;

// Authenticated user Box Section
export const currentHostLabel: HTMLElement = document.getElementById(
  "current-host-label"
) as HTMLSpanElement;

// Connected account address
export const accountAddressLabel: HTMLElement = document.getElementById(
  "account-address-label"
) as HTMLSpanElement;

// Connected account avatar image
export const accountAvatarImage: HTMLImageElement = document.getElementById(
  "account-avatar-image"
) as HTMLImageElement;
