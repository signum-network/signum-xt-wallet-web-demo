// Connect status Label
export const connectionStatusLabel = document.getElementById(
  "connection-status-label"
);

// Selected Network Label
export const selectedNetworkLabel = document.getElementById(
  "selected-network-label"
);

// Required Network Label
export const requiredNetworkLabel = document.getElementById(
  "required-network-label"
);

// Connect Wallet Button
export const connectWalletButton = document.getElementById(
  "connect-wallet-button"
);

// Disconnect Wallet Button
export const disconnectWalletButton = document.getElementById(
  "disconnect-wallet-button"
);

// Authenticated user Box Section
export const authenticatedUserBox = document.getElementById(
  "authenticated-user-box"
);

// Watch only account label
export const watchOnlyAccountLabel = document.getElementById(
  "watch-only-account-label"
);

// Current host label
export const currentHostLabel = document.getElementById("current-host-label");

// Connected account address
export const accountAddressLabel = document.getElementById(
  "account-address-label"
);

// Connected account avatar image
export const accountAvatarImage = document.getElementById(
  "account-avatar-image"
);

// Container of button for sending messages
export const messageButtonContainer = document.getElementById(
  "message-buttons-container"
);

// Send message button
export const sendMessageButton = document.getElementById("send-message-button");

// Send encrypted message button
export const sendEncryptedMessageButton = document.getElementById(
  "send-encrypted-message-button"
);

// Successful transaction box
export const successfulTransactionBox = document.getElementById(
  "successful-transaction-box"
);

// Container Sign TransactionModal
export const signTransactionModal = document.getElementById(
  "sign-transaction-modal"
);

// XT Wallet Store Url anchor (It is located on the footer)
export const xtWalletStoreUrlAnchor = document.getElementById(
  "xt-wallet-store-url-anchor"
);
